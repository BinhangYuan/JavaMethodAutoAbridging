import React from 'react'
import {connect} from 'react-redux'

export const Question = ({questionObject,dispatch})=>{
	let index;
	return (
		<div class="jumbotron text-center">
  			<h3>User Study</h3>
		</div>
	)
}